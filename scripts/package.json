{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Utility scripts for local testing live in this folder. Each script reads database configuration from the same `.env` file that the backend uses via `backend/src/config/database.js`.",
  "main": "get-popular-students.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.4.1",
    "mongoose": "^9.4.1"
  }
}
