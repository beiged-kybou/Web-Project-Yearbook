import { useState } from 'react';\nimport './Input.css';\n\nconst Input = ({\n  type = 'text',\n  value,\n  onChange,\n  placeholder,\n  label,\n  helperText,\n  error,\n  required = false,\n  disabled = false,\n  icon,\n  ...props\n}) => {\n  const [isFocused, setIsFocused] = useState(false);\n  const [isTouched, setIsTouched] = useState(false);\n\n  const handleBlur = () => {\n    setIsFocused(false);\n    setIsTouched(true);\n  };\n\n  const showError = error && isTouched;\n\n  return (\n    <div className=\"input-wrapper\">\n      {label && (\n        <label className={`input-label ${isFocused ? 'focused' : ''}`}>\n          {label}\n          {required && <span className=\"required-asterisk\">*</span>}\n        </label>\n      )}\n      \n      <div className={`input-container ${showError ? 'error' : ''} ${isFocused ? 'focused' : ''}`}>\n        {icon && <span className=\"input-icon\">{icon}</span>}\n        <input\n          type={type}\n          value={value}\n          onChange={onChange}\n          onFocus={() => setIsFocused(true)}\n          onBlur={handleBlur}\n          placeholder={placeholder}\n          disabled={disabled}\n          required={required}\n          className={`${icon ? 'has-icon' : ''}`}\n          {...props}\n        />\n      </div>\n      \n      {(helperText || showError) && (\n        <div className={`helper-text ${showError ? 'error' : ''}`}>\n          {showError ? error : helperText}\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default Input;\n
